#!/bin/bash

wget -q https://github.com/devops-works/binenv/releases/latest/download/binenv_linux_arm64 -O binenv
chmod +x binenv
./binenv update --cache
./binenv update binenv
./binenv install binenv
rm binenv
