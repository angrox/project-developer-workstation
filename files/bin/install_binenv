#!/bin/bash

arch=${1:-"amd64"}


wget -q "https://github.com/devops-works/binenv/releases/latest/download/binenv_linux_${arch}" -O binenv
chmod +x binenv
./binenv update --all
./binenv install binenv
rm binenv
